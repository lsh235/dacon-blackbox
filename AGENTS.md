# 프로젝트 작업 지침

## 적용 범위

- 이 지침은 이 디렉터리와 모든 하위 디렉터리에 적용한다.
- `DOC/`는 대회 원본 자료를 보관하는 **읽기 전용 참고 디렉터리**다.
- 사용자가 `DOC/` 자체의 변경을 명시적으로 요청하지 않는 한 그 안의 파일과 디렉터리를 수정, 이동, 삭제하거나 이름을 바꾸지 않는다.

## `DOC/` 자료의 역할

다음 문서를 프로젝트의 공식 참고 기준으로 사용한다. 구현·수정·검증을 시작하기 전에 작업과 직접 관련된 문서를 먼저 읽고, 문서에 명시되지 않은 요구사항은 임의로 추가하지 않는다.

- `DOC/Overview/Overview.md`: 대회 목적, 세 가지 Stage의 분석 대상, 대회 진행 방식, 평가 서버 사양 및 전체 시간·용량 제한을 확인하는 기준 문서다.
- `DOC/Overview/Evaluation.md`: Stage별 점수 산식, 세부 평가 항목·가중치, 오답 처리 기준 및 1·2차 평가 기준을 확인하는 기준 문서다.
- `DOC/Overview/SubmitCodeGuide.md`: `submit.zip` 구조, 필수 파일과 함수, 출력 DataFrame/CSV 컬럼, 평가 서버에서 추가되는 경로, 오프라인 실행 규칙 및 제출 제한을 확인하는 기준 문서다.
- `DOC/Data/Overview.md`: Stage별 입력 데이터 구조, 파일·폴더·레이블 의미, 출력 형식 및 baseline 예제의 해석 범위를 확인하는 기준 문서다.
- `DOC/Data/detail/`: `DOC/Data/Overview.md`에서 언급하는 `baseline.zip`의 압축 해제 파일을 보관하는 참고 디렉터리다. baseline 학습·추론 노트북, 요구사항, 샘플 데이터의 실제 구조와 동작을 확인할 수 있지만, baseline 구현은 공식 입출력 계약을 이해하기 위한 예시로만 취급한다.

대회 기준이 서로 다르게 보이면 다음 우선순위를 따른다: 제출·평가 계약(`SubmitCodeGuide.md`, `Evaluation.md`) → 대회·데이터 설명(`Overview.md`) → baseline 예제(`DOC/Data/detail/`). 충돌이나 모호함이 해소되지 않으면 임의로 결정하지 말고 관련 파일 경로와 내용을 사용자에게 보고한다.

## 참고 자료 보호 규칙

- `DOC/` 안에서 노트북을 직접 실행하지 않는다. 실행이 필요하면 개발 디렉터리로 복사한 뒤 실행하고, 체크포인트·로그·캐시·노트북 출력 등 생성물도 `DOC/` 밖에 저장한다.
- 개발 코드가 실행 중 `DOC/` 내부 파일을 import하거나 고정 경로로 참조하게 만들지 않는다.
- 샘플 코드나 설정을 활용할 때는 `DOC/` 밖의 개발 영역으로 복사·재작성하고, 출처가 된 파일 경로를 추적할 수 있게 남긴다.
- `DOC/`에 가상환경, 다운로드 파일, 모델 가중치, 임시 데이터, 제출 ZIP 또는 기타 개발 산출물을 추가하지 않는다.
- `DOC/`의 예제 데이터는 형식과 동작을 이해하기 위한 자료로 취급한다. 원본 파일을 전처리 결과로 덮어쓰거나 학습·평가 과정에서 변경하지 않는다.

## 개발 및 검증 절차

1. 구현 전에 `DOC/Overview/Overview.md`에서 대회 목표, Stage 범위, 실행 환경 및 전체 제약을 확인한다.
2. `DOC/Overview/Evaluation.md`에서 목표 지표, Stage별 가중치, 허용 범위 및 오류 처리 기준을 확인한다.
3. `DOC/Overview/SubmitCodeGuide.md`에서 제출 구조, 추론 함수 계약, 출력 컬럼, 오프라인 조건 및 시간·용량 제한을 확인한다.
4. `DOC/Data/Overview.md`에서 Stage별 실제 입력 구조, 레이블 의미, 샘플과 평가 데이터의 차이를 확인한다.
5. 필요한 경우 `DOC/Data/detail/`의 baseline 파일을 개발 영역으로 복사해 구조와 동작을 참고한다. baseline의 미학습 출력·파생 예제·하드코딩된 값은 실제 성능이나 공식 정답으로 간주하지 않는다.
6. 실제 소스, 설정, 테스트, 실험 결과와 제출물은 모두 `DOC/` 밖에서 작성한다.
7. 기능 검증은 각 Stage의 입출력 계약과 평가 조건을 기준으로 수행하고, 실행 성공·형식 검증·실제 성능 측정 결과를 서로 구분해 기록한다.
8. 제출 전에는 실행 환경, 오프라인 실행 여부, 패키지 설치, 제한 시간, 파일 크기, 압축 구조와 최종 출력 형식을 위 기준 문서와 다시 대조한다.

## 해석 및 충돌 처리

- 자료에 없는 요구사항이나 데이터 의미를 추측해서 구현하지 않는다.
- 문서 사이에 내용이 다르거나 모호한 부분이 있으면 임의로 하나를 선택하지 말고, 충돌한 파일 경로와 내용을 사용자에게 보고한 뒤 확인한다.
- 공식 대회 공지가 `DOC/`보다 최신임이 확인되면 최신 공지를 우선하되, 달라진 내용과 영향 범위를 사용자에게 명시한다.
- 개발 편의를 위한 변경이 대회 규칙, 평가 방식 또는 제출 형식을 바꾸지 않도록 한다.
